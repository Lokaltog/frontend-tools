{
  "name": "@heetch/jest-mock-openapi",
  "version": "1.0.0",
  "description": "Helper to mock and validate fetch requests with OpenAPI (Swagger) contracts",
  "main": "./dist/index.js",
  "repository": "https://github.com/heetch/frontend-tools",
  "author": "Heetch",
  "license": "MIT",
  "scripts": {
    "build": "copyfiles -f ./src/*.js ./dist",
    "prepare": "yarn test && npm run build",
    "test": "jest"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}